# DeskLine
Multi-role helpdesk ticketing platform with JWT auth, RBAC, audit logging, and SLA tracking — built with ASP.NET Core, React (TypeScript), and PostgreSQL, deployed with a full CI/CD pipeline.
